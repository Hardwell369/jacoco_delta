import argparse
from jacoco_delta.workflow.runner import WorkflowRunner as Runner

def main():
    # parser = argparse.ArgumentParser(description="Jacoco Delta Analysis Tool")
    # parser.add_argument("base", help="Path to the base Jacoco report")
    # parser.add_argument("head", help="Path to the head Jacoco report")
    # parser.add_argument("output", help="Path to the output report")
    # args = parser.parse_args()

    # runner = Runner(args.base, args.head, args.output)
    # runner.run()
    pass